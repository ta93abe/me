---
// PostHog analytics。bundled script にして Astro CSP ハッシュ対象にする。
// 本番ビルドかつトークンがあるときのみ読み込む。
const enabled =
	import.meta.env.PROD && Boolean(import.meta.env.PUBLIC_POSTHOG_PROJECT_TOKEN);
---

{
	enabled && (
		<script>
			import "../scripts/posthog";
		</script>
	)
}
